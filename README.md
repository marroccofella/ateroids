# @TEROIDS — Arcade Word Shooter

**[Play on GitHub Pages](https://marroccofella.github.io/ateroids/)** · [42.uk Library](https://42.uk/library.html)

> `RELAX. IT'S ALREADY OVER.`
>
> A rogue application from **[The Library](https://42.uk)** — an unstable collection of
> anomalous software generated by the very chaos we're navigating.

Classic asteroid physics with collectible letters. Shoot rocks to release letters,
collect them with your ship for word scores, and deploy a smart bomb when the field
gets too crowded.

## Controls

| Input | Action |
| --- | --- |
| `←` `→` | Rotate ship |
| `↑` | Thrust |
| `Space` | Shoot |
| `B` | Smart bomb |
| `P` / `Escape` | Pause or resume |

On-screen controls support touch, mouse and keyboard activation. The game is silent. It pauses when the tab loses focus. The included starter dictionary contains 39 words.

## Run it

No build step. It's one canvas, one script, zero excuses:

```bash
# any static server works
npx serve .
```

Then open `http://localhost:3000`.

## Stack

- Vanilla JavaScript + `<canvas>` — the whole game lives in `game.js`
- Leftover Firestore config is locked down (`allow read, write: if false`); nothing
  server-side ships until a leaderboard actually does

---

Part of the **42.uk** ecosystem — *The Freeloader's Guide to the Undeterministic
Universe*. Explore the rest of [The Library](https://42.uk).
